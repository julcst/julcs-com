{
  "name": "julcs-com",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate-types": "wrangler types",
    "deploy": "pnpm run build && wrangler pages deploy dist --project-name julcs-com"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "@fontsource/jetbrains-mono": "^5.2.8",
    "astro": "^6.3.1",
    "astro-favicons": "^3.1.6",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "wrangler": "^4.90.0"
  },
  "overrides": {
    "vite": "^7"
  }
}